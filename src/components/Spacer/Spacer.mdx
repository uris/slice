import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv} from '../FlexDiv';
import * as SpacerStories from './Spacer.stories';
import {RenderSource} from '../../../documentation/_components/RenderSource/RenderSource';

<Meta of={SpacerStories} />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
        # Spacer
        Renders a fixed square of empty space using the theme spacing grid.

        ## Base element
        `HTMLDivElement`

        ## Interface
        `SpacerProps`

        ## Props
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '1%'}} />
            <col style={{width: '97%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Prop</th>
              <th className={"left"}>Type</th>
              <th className={"left"}>Default</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>size</code></td><td><code>xxs | xs | s | m | l | xl | xxl | h</code></td><td><code>s</code></td><td>Theme spacing token for both width and height (2, 4, 8, 16, 24, 32, 64, 128px by default). Numeric sizes are no longer supported.</td></tr>
            <tr><td><code>...divAttributes</code></td><td><code>{`HTMLAttributes<HTMLDivElement>`}</code></td><td><code>-</code></td><td>Native `div` props pass through (for example `id`, `data-testid`, `className`, `style`, `aria-*`).</td></tr>
          </tbody>
        </table>

        ## Events
        <table style={{width: '100%'}}>
          <colgroup>
            <col style={{width: '1%'}} />
            <col style={{width: '99%'}} />
          </colgroup>
          <thead>
            <tr>
              <th className={"left"}>Event</th>
              <th className={"left"}>Type</th>
            </tr>
          </thead>
          <tbody>
            <tr><td><code>-</code></td><td><code>-</code></td></tr>
          </tbody>
        </table>

        ## Code Examples
        </Unstyled>

        <RenderSource language={"tsx"}>
```
import { Spacer } from '@apple-pie/slice';

export function SpacerExample() {
    return (
        <Spacer
            size="l"
            id="spacer-example"
            data-testid="spacer-example"
            className="custom-wrapper"
            style={{ marginTop: 8, maxWidth: 480 }}
        />
    );
}
```
        </RenderSource>
    </FlexDiv>
</FlexDiv>
