import {Meta, Unstyled} from '@storybook/addon-docs/blocks';
import {FlexDiv, Spacer} from "../src";
import {TypeTokenTable} from "./_components/TypeTokenTable/TypeTokenTable";
import {fontWeightTokens, fontSizeTokens, lineHeightTokens, letterSpacingTokens} from "./_data/typeTokenData";

<Meta title="Typography/Slice Type Sizes" />

<FlexDiv width={"fill"} height={"fill"} align={"center"}>
    <FlexDiv width={"fill"} maxWidth={760} height={"auto"}>
        <Unstyled>
            # Type Sizes
            Every ready-made style on the [Type Styles](/docs/typography-slice-type-styles--docs) page is built from a small set of granular tokens - font weight, font size, line height, and letter spacing. Each one is exposed as its own CSS custom property, so you can mix and match them to compose a custom type style without starting from scratch.

            #### Font Size
            <TypeTokenTable tokens={fontSizeTokens} />

            #### Font Weight
            <TypeTokenTable tokens={fontWeightTokens} />
            
            #### Line Height
            <TypeTokenTable tokens={lineHeightTokens} />

            #### Letter Spacing
            <TypeTokenTable tokens={letterSpacingTokens} />
        </Unstyled>
        <Spacer size={64}/>
    </FlexDiv>
</FlexDiv>
